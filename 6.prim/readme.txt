prim is used to get the Minimum tree of the undirected graph.(prim算法用于求无向图的最小生成树)

具体思想是:
1.初始化将某个节点u，及其相连的边加入到边数组tmp中,并将已获得边数num值为1.

2.然后从已有边数组tmp中找到一条最小边，这条边有一个顶点u已经被确定被加入到了最小生成树，另一顶点v未访问过。

3.将该未访问的顶点v标记为已加入到生成树，num值加1，并将与顶点v相连的边(该边中有一个顶点未加入到生成树)
加入到边数组tmp中。

4.重复2,3步骤，直到所有的顶点都加入到了生成树。